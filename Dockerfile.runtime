# Runtime-only image (no build, binary mounted via volume)
FROM alpine:3.19

RUN apk add --no-cache ca-certificates tzdata

WORKDIR /app

RUN mkdir -p /app/data/assets /app/bin

COPY internal/static ./internal/static
COPY docs/openapi.yaml ./docs/

EXPOSE 8080

ENTRYPOINT ["./bin/kc-server"]
